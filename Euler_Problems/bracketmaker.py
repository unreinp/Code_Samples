def main():
    names = ['sean', 'emma', 'peter', 'james',]
    bracket = []
    bracket = rtnBracker(names,bracket)

def rtnBracket(names,bracket):
    bracket += [(names.pop(0),names.pop(len(names)))]
    return bracket
def makeBIlist(names, seed)
    if len(names) != len(seed):
        print('namelist and seed list different lengths')
        return -1
    for i in range(len(names)):
        BIList += [bracketIndex(names(i),seeds(i))]
class bracketIndex():
    def __init__(name, seed):
        self.name = name
        self.seed = seed
    def getSeed(self):
        return self.seed
    def __repr__ (self):
        return str(self.name) +str(self.seed)
def bracketTree():
    def __init__(BIList):
        
main()
